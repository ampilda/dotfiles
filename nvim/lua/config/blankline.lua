local ibl = require("ibl")

ibl.setup {
    indent = { char = "·" }, 
    scope = { enabled = false }, 
    whitespace = {
        highlight = "Whitespace", 
        remove_blankline_trail = false, 
    }
}

